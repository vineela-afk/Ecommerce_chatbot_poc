
server:
  port: 8080

spring:
  kafka:
    bootstrap-servers: localhost:9092

resilience4j:
  retry:
    instances:
      aiService:
        maxAttempts: 3
        waitDuration: 1000
  circuitbreaker:
    instances:
      aiService:
        failureRateThreshold: 50
        slidingWindowSize: 10
